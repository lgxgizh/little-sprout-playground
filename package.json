{
  "name": "little-sprout-playground",
  "version": "0.1.0",
  "private": true,
  "description": "A picture-first, voice-assisted learning web app for preschool children.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lgxgizh/little-sprout-playground.git"
  },
  "homepage": "https://lgxgizh.github.io/little-sprout-playground/",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm test && node --check src/app.js && node --check src/learning-plan.js && node --check src/storage.js && vite build"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "vite": "^6.1.0"
  }
}
