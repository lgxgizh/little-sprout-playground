# Optional: URL of your trusted server-side model adapter.
# Never put provider API keys in VITE_* variables or commit real secrets.
# Leave empty to use the fully local demo and automatic fallback.
VITE_API_BASE_URL=http://localhost:3000/api
